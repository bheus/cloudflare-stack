services:
  cloudflared:
    image: cloudflare/cloudflared:2026.8.3
    container_name: cloudflared-tunnel
    restart: unless-stopped
    command: tunnel --no-autoupdate run brendan-website
    volumes:
      - /etc/cloudflared:/etc/cloudflared:ro
    network_mode: host
